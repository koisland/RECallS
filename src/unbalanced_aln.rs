// fn is_unbalanced_alignment(mismatch_pos: &[usize], read_len: usize) -> bool {
//     if mismatch_pos.is_empty() {
//         return false;
//     }
//     let midpt = read_len / 2;
//     let itv_left = (0usize, midpt - 1);
//     let itv_right = (midpt, read_len);

//     if !mismatch_pos.is_empty() {
//     } else {
//     }
//     todo!()
// }
